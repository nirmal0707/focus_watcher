import 'package:flutter/widgets.dart';

class FocusWatcher extends StatelessWidget {
  const FocusWatcher({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
